<?xml version="1.0" encoding="utf-8" ?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Lista_videojuegos.Models"
             xmlns:vm="clr-namespace:Lista_videojuegos.ViewModels"
             x:Class="Lista_videojuegos.Views.FavoritosPage"
             x:DataType="vm:FavoritosViewModel"
             Title="Wishlist">

    <VerticalStackLayout Padding="10">

        <Label Text="Mi Wishlist"
               FontSize="24"
               FontAttributes="Bold"
               Margin="10" />

        <CollectionView ItemsSource="{Binding Productos}"
                        SelectionMode="None">

            <CollectionView.EmptyView>

                <VerticalStackLayout Padding="20"
                                     VerticalOptions="Center"
                                     HorizontalOptions="Center">

                    <Label Text="Tu Wishlist está vacía."
                           FontSize="18"
                           HorizontalTextAlignment="Center" />

                    <Label Text="Agrega juegos desde la pantalla de Detalle o con el formulario."
                           FontSize="14"
                           TextColor="Gray"
                           HorizontalTextAlignment="Center" />

                </VerticalStackLayout>

            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>

                <DataTemplate x:DataType="models:Videojuego">

                    <Grid Padding="10"
                          ColumnDefinitions="80,*,Auto">

                        <Image Source="{Binding ImagenUrl}"
                               HeightRequest="70"
                               WidthRequest="70"
                               Aspect="AspectFill"
                               Grid.Column="0" />

                        <VerticalStackLayout Grid.Column="1"
                                             Margin="10,0,0,0"
                                             VerticalOptions="Center">

                            <Label Text="{Binding Nombre}"
                                   FontSize="16"
                                   FontAttributes="Bold" />

                            <Label Text="{Binding Precio, StringFormat='${0:N2}'}"
                                   FontSize="15"
                                   TextColor="DarkGreen"
                                   FontAttributes="Bold" />

                        </VerticalStackLayout>

                        <Button Text="Eliminar"
                                Grid.Column="2"
                                VerticalOptions="Center"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EliminarFavoritoCommand}"
                                CommandParameter="{Binding .}" />

                    </Grid>

                </DataTemplate>

            </CollectionView.ItemTemplate>

        </CollectionView>

    </VerticalStackLayout>

</ContentPage>